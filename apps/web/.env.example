# Kiwi Lens Web is deployed with the Cloudflare Worker on the same origin.
# Google Maps configuration is loaded at runtime from /api/config.
# Do not put Google Maps API keys in Vite environment variables.
